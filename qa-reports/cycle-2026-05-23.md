# Cycle Briefing — 2026-05-23 (Day-0 Kickoff)

**Project:** AI Portfolio Website
**Cycle branch:** `cycle/auto-2026-05-23` (no commits yet — every artifact is staged-but-uncommitted; `main` is intentionally unborn)
**Mode:** Kickoff planning ONLY. Zero production code. Zero `main` commits. Zero external sends.
**Compiled by:** Morgan (orchestrator-mode, no external sends per Constitution v1.3 Art. 9.4)
**Roles that ran:** Quinn, Dani, Riley, Rory, Dana, Shamus, Alex, Will
**Roles deliberately skipped:** Peter, Gary, Steve, Casey, Jordan (Casey/Jordan only re-engage if Sky enables a contact form or Journal)

---

## 1. DECISIONS FOR SKY

**Total: 19 decisions across 5 themes.** Lead with these. Items marked **[BLOCKS CYCLE 2]** must be answered before Shamus can run `npx create-next-app`. Items marked **[LATER OK]** can be answered before the cycle that actually needs them.

### Theme A — Project setup (gates Cycle 2)

**A1. Confirm local repo location: `~/Portfolio` (capital P).** **[CONFIRM OR REDIRECT]**
- Raised by: Morgan / Rory.
- Recommendation: Keep `~/Portfolio` — repo already exists here, easy to move if wrong, but no reason to.
- Downstream: `basePath` in `next.config.mjs` (see A2 — case sensitivity matters); all import aliases.

**A2. GitHub repo name.** **[BLOCKS CYCLE 2 — Rory cannot finalize `basePath`]**
- Raised by: Rory (DEPLOY_PLAN §1.1, §7 decision #1).
- Options: `portfolio` (lowercase, recommended) → `https://skypie99.github.io/portfolio/`; OR `Portfolio` (case-sensitive match to local folder) → `https://skypie99.github.io/Portfolio/`.
- Recommendation: **`portfolio` (lowercase)** — cleaner URL, no case-sensitivity footguns on Linux build hosts.
- Downstream: hard-codes the value of `basePath: '/portfolio'` (or `/Portfolio`, or `''` if custom domain) in `next.config.mjs`. Wrong value breaks every asset URL on GH-Pages. **Conflict to surface:** Shamus's SCAFFOLDING_PLAN §2 currently writes `basePath: '/Portfolio'` (capital P) as a placeholder — Sky's answer overwrites this verbatim.

**A3. Ratify tech stack: Next.js 15 (App Router, static export) + TypeScript strict + Tailwind v3 + Framer Motion v11 + Lucide icons.** **[BLOCKS CYCLE 2]**
- Raised by: Shamus (SCAFFOLDING_PLAN §1, §8 decisions #1 & #2).
- Recommendation: **Approve as proposed.**
- Downstream: Tailwind no = +~2 days CSS Modules rewrite across Cycles 2–6. Framer Motion no = +~2 days bespoke IntersectionObserver/CSS motion across Cycles 3 & 6. Both are reversible later but expensive to swap.

**A4. Content format for deliverables: MDX (one `.mdx` per deliverable) vs. JSON-only.** **[BLOCKS CYCLE 4 — not Cycle 2]**
- Raised by: Dana (DATA_SHAPE §7 decision #1), echoed by Shamus (SCAFFOLDING_PLAN §8 decision #6).
- Conflict to flag: **Dana recommends MDX** (richer F-05 detail pages, no future schema migration). **Shamus recommends JSON for v1** (defer MDX until Journal ships). Both are defensible — the split is whether F-05 will grow into long-form case studies.
- Recommendation: Morgan leans toward Dana's call (MDX for deliverables, JSON for profile + certificates) — F-05 is core P0 work and benefits from real narrative; Shamus's "defer" rationale was tied to MDX-only-if-Journal, which is no longer the gating concern.
- Downstream: yes = `@next/mdx` or `next-mdx-remote` added to package.json (~50KB build-time only); no = `longDescription` string field in Deliverable schema, prose escaped into JSON.

**A5. `main` branch lifecycle.** **[CONFIRM]**
- Raised by: Morgan / Rory.
- Recommendation: When Sky is ready to bless this cycle, run `git checkout cycle/auto-2026-05-23 && git add -A && git commit -m "..." && git branch main && git push -u origin main` (or equivalent). Per Constitution Art. 1, only Sky touches `main`. Note: the cycle branch currently has zero commits — Sky must commit the staged artifacts before/at merge.
- Downstream: triggers GH-Pages deploy ONLY after Cycle 6's actual app code lands. Day-0 merge is just docs; harmless.

### Theme B — Brand & accessibility (Dani/Alex collaboration)

**B1. Option B brand-color split: Terracotta `#B35F32` graphics-only + Umber `#7F4323` for link text and 19px numerals.** **[CONFIRM — Dani already applied; needs Sky's blessing]**
- Raised by: Alex (ACCESSIBILITY §9.1 SKY-1), applied by Dani (PROJECT_DESIGN Changelog + §1.1 + §3.6).
- Recommendation: **Approve Option B (already shipped in PROJECT_DESIGN).** Options C (darken master Terracotta to `#9F4F23`) and D (separate `--color-link`) are also WCAG-compliant alternatives if Sky prefers; Option A (keep Terracotta as link text) is a Constitution Art. 7 violation and off the table.
- Downstream: visual difference reads as "deeper orange" rather than a new hue — brand preserved. Option C shifts the master Terracotta token, which would ripple through every reference; B is the safer surgical fix Dani has already made.

**B2. Two-token hairline split: `--color-border` decorative `#DCDCD6` Stone + `--color-border-interactive` `#888879`.** **[CONFIRM — Dani already applied]**
- Raised by: Alex (ACCESSIBILITY §9.2 SKY-2), applied by Dani (PROJECT_DESIGN §1.1, §3.2).
- Recommendation: **Approve the two-token split** (Alex's vote, Dani applied). Alternative: standardize on `#888879` for ALL borders — simpler palette but slightly muddies editorial minimalism.
- Downstream: ghost buttons, future inputs, focus surrounds use the interactive token; section dividers and decorative hairlines stay on Stone. No effect on visible aesthetic for sighted users at normal viewing distance.

**B3. Cormorant Garamond Light 300 restricted to ≥24px display tiers; Regular 400 mandated below.** **[CONFIRM — Dani already applied]**
- Raised by: Alex (ACCESSIBILITY §7.2), applied by Dani (PROJECT_DESIGN Changelog + §2.2 rules).
- Recommendation: **Approve.** Visible aesthetic shift: 19px card titles and sidebar wordmark will read slightly heavier than ffern's hairline reference. Trade is legibility for low-vision visitors and low-density screens.
- Downstream: Shamus must enforce `font-weight: 400` on `.card-title` and `.wordmark` in CSS (already flagged in SCAFFOLDING_PLAN §5).

**B4. Brand Extensions (Petal Pink, Moss, Earth, Wildflower) — hold for v2 or include in v1?** **[LATER OK]**
- Raised by: Dani (PROJECT_DESIGN §1.5, §7.3.1).
- Recommendation: **Hold for v2.** Single-accent restraint is core to ffern's read; only add when v2 needs categorical color coding (e.g., tagging deliverables by domain).
- Downstream: if Sky approves later, every new color needs a fresh contrast audit against every page surface (Alex flagged this in ACCESSIBILITY §10).

**B5. Hamburger touch target: 40×40 visual + 44×44 hit area (expand via padding) vs. bump visual to 44×44.** **[LATER OK — Cycle 2]**
- Raised by: Alex (ACCESSIBILITY §3.5).
- Recommendation: **40×40 visual + 44×44 invisible hit area** (Alex's pick — preserves Dani's restraint). Passes WCAG 2.5.8 AA either way.
- Downstream: Shamus implements via padding wrapper around the 22px glyph.

### Theme C — Content (Sky has to supply real data)

**C1. Real deliverables list + hero images + metadata.** **[BLOCKS CYCLE 4 — placeholders OK for scaffolding]**
- Raised by: Quinn (FEATURES decision #3), Dana (DATA_SHAPE §7), Shamus (SCAFFOLDING_PLAN §8 decision #5).
- Recommendation: Sky to provide a list of 3–5 real AI deliverables with: title, summary (≤160 chars), role, tech list, year, hero image (or willingness to source per Dani §6 photography rules), optional external links. Placeholders in Cycle 4 work for visual review; launch quality depends on real content.
- Downstream: until Sky provides, Shamus uses the two stub examples in DATA_SHAPE §3.3–3.4 (Voice Coach, Accessibility Pulse) plus a third placeholder.

**C2. Real certificates list + credential URLs + badge images.** **[BLOCKS CYCLE 5 — placeholders OK]**
- Raised by: Quinn (FEATURES decision #3), Dana (DATA_SHAPE §7).
- Recommendation: Sky to provide every certificate's: title, issuer, ISO issued date, credential URL, badge image (or wordmark fallback).
- Downstream: F-06 spec requires a working "View credential" external link per certificate; placeholders without real URLs won't pass Quinn's acceptance criteria.

**C3. Which single deliverable goes in F-02 sidebar's Featured slot at launch?** **[LATER OK — Cycle 4]**
- Raised by: Quinn (FEATURES decision #4), echoed by Dana (DATA_SHAPE §7 decision #4 + §6 featured-slot invariant).
- Recommendation: Sky picks one deliverable's `featured: true` flag after Cycle 4 cards exist; the Zod validator (DATA_SHAPE §6) enforces exactly-one across all deliverable files.
- Downstream: if zero are featured, sidebar gracefully falls back to a "Latest Work →" link (Dana built this fallback in). If two or more, the build fails with a clear error listing the offenders.

**C4. Surface certificate `expiresDate` in the UI?** **[LATER OK — Cycle 5]**
- Raised by: Dana (DATA_SHAPE §7 decision #2).
- Options: A (default) store expiry in schema but don't render; B render "Issued YYYY · Expires YYYY" on every cert that has expiry.
- Recommendation: **Option A.** Most viewers don't care; future-dated expiry can read as either reassuring or as a freshness countdown. Reversible later without data migration.

**C5. Tags: free-form (Sky writes whatever) vs. controlled vocabulary?** **[LATER OK — Cycle 4]**
- Raised by: Dana (DATA_SHAPE §7 decision #3).
- Recommendation: **Free-form for v1.** No tag-filter UI is in the backlog; tags are visual labels only. Revisit only if/when filter UI is groomed.

### Theme D — Feature scope

**D1. Contact: mailto-only (Quinn's recommendation) vs. real form.** **[BLOCKS CYCLE 6 — F-08]**
- Raised by: Quinn (FEATURES decision #1), echoed by Riley (PERSONAS — Maya/Priya/Jordan all prefer email over form), echoed by Rory (DEPLOY_PLAN §4.1 — static export can't use Next API routes, so a form needs a third-party service).
- Recommendation: **Mailto-only for v1.** Privacy-trivial; matches three of four personas' expectations; zero infrastructure cost. If Sky later wants a form, Jordan must spec retention + consent BEFORE a single field is built (Quinn's gate).
- Downstream: if Sky says form, Jordan re-engages next cycle and timeline slips ~1 cycle.

**D2. Journal / Blog (F-09): ship or skip?** **[BLOCKS CYCLE 7+ ONLY — not Cycle 2]**
- Raised by: Quinn (FEATURES decision #2 & #5), echoed by Shamus (SCAFFOLDING_PLAN — `(journal)` route group conditional).
- Recommendation: **Defer (P2)** until Sky commits to publishing 3+ posts in the next 90 days. Skipping also lets Quinn remove the Journal nav item entirely (don't ship a dead link).
- Downstream: yes = adds MDX dependency (`next-mdx-remote` + `gray-matter`, ~1 extra cycle to ship); ongoing authoring commitment. No = clean palette of P0/P1 features only.

**D3. Footer columns — three columns as Quinn specced (Site / About / Social) — confirm which social profiles to list publicly.** **[LATER OK — Cycle 6]**
- Raised by: Quinn (F-10 dependencies), echoed by Dana (Profile.socials).
- Recommendation: Sky picks from the supported `platform` enum: github, linkedin, twitter, mastodon, bluesky, other. At minimum: GitHub + LinkedIn (the two already in Dana's example).

### Theme E — Deploy

**E1. Public vs. private repo.** **[BLOCKS CYCLE 2 setup]**
- Raised by: Rory (DEPLOY_PLAN §7 decision #4).
- Recommendation: **Public.** Free GH-Pages requires public; portfolio is public-facing anyway. Private repos need GH Pro.

**E2. Custom domain or stay on `skypie99.github.io/portfolio/`?** **[LATER OK]**
- Raised by: Rory (DEPLOY_PLAN §3.4, §7 decision #2).
- Recommendation: **Defer.** Ship to GH-Pages subdir first; add domain after content lands. Custom domain = DNS verification + CNAME file + `basePath: ''` change.

**E3. Node version: 20 (current LTS) — confirm.** **[CONFIRM]**
- Raised by: Rory (DEPLOY_PLAN §7 decision #5).
- Recommendation: **Approve.** Matches `engines` field Shamus will set, CI runner, local dev.

---

## 2. What shipped this cycle

**Code shipped: ZERO. Commits to `main`: ZERO. External sends: ZERO.** This is a planning cycle — every artifact is a proposal that becomes binding only on Sky's ratification and merge.

All artifacts live on branch `cycle/auto-2026-05-23` as untracked files in the working tree (Sky's first commit will introduce them):

| Artifact | Author | Purpose |
|---|---|---|
| `/Users/skypie/Portfolio/PLAN.md` | Morgan | Cycle plan, dependency graph, skipped-roles rationale, deferred decisions |
| `/Users/skypie/Portfolio/README.md` | Will | Repo orientation; recommended reading order |
| `/Users/skypie/Portfolio/.gitignore` | Morgan | Standard Next.js ignore patterns |
| `/Users/skypie/Portfolio/docs/FEATURES.md` | Quinn | 10-item backlog (F-01 to F-10), 5 cross-cutting acceptance criteria, 5 escalations |
| `/Users/skypie/Portfolio/docs/PROJECT_DESIGN.md` | Dani | 65-token design system + 9 component primitives + photography direction; Changelog records Alex's WCAG fixes; 5 escalations in §7.3 |
| `/Users/skypie/Portfolio/designs/home-hero-mockup.md` | Dani | ASCII desktop + mobile homepage wireframes with token-mapped annotations |
| `/Users/skypie/Portfolio/docs/PERSONAS.md` | Riley | 4 composite personas (Maya, Daniel, Priya, Jordan) + 3 critical journeys + double-edged ffern-aesthetic finding |
| `/Users/skypie/Portfolio/docs/DEPLOY_PLAN.md` | Rory | GH-Pages workflow shape, `basePath` rules, `.nojekyll`, rollback plan, 5 escalations |
| `/Users/skypie/Portfolio/docs/DATA_SHAPE.md` | Dana | Deliverable + Certificate + Profile Zod schemas, MDX/JSON trade-off, featured-slot integrity rule, 3 escalations |
| `/Users/skypie/Portfolio/docs/SCAFFOLDING_PLAN.md` | Shamus | Stack confirmation, init commands, file tree, component→token map, Cycles 2–6 build order, 6 escalations |
| `/Users/skypie/Portfolio/docs/ACCESSIBILITY.md` | Alex | WCAG 2.2 AA contrast audit (16 pairs), 3 BLOCKERs raised (since resolved), 2 escalations to Sky |
| `/Users/skypie/Portfolio/docs/LEARNINGS.md` | Will | Day-0 skeleton; intentionally empty until Cycle 2+ surfaces real gotchas |
| `/Users/skypie/Portfolio/qa-reports/cycle-2026-05-23.md` | Morgan | This briefing |

---

## 3. Per-role status

**Quinn (PM).** Shipped 10-feature backlog with priorities (4×P0, 4×P1, 1×P2 + 1 deferred-pending-decision), value/effort ranking, 5 cross-cutting acceptance criteria (C-1 to C-5: cream-not-white, no-bold, editorial photo, WCAG AA, single-narrative-per-screen), and a clean Definition of Done that includes Alex sign-off + Gary test coverage. Headline finding: F-08 (Contact) and F-09 (Journal) are both gated on Sky's call; recommends mailto + defer Journal. Not blocked.

**Dani (Creative Director).** Shipped 65-token design system + 9 component primitives + ASCII homepage wireframe. Headline finding: the warm-minimal vibe survives Alex's audit without losing brand integrity — Terracotta stays as the brand color for graphics/CTAs/focus rings; Umber (already in palette) takes over inline links and small numerals. Revised PROJECT_DESIGN.md mid-cycle in response to Alex's 3 blockers (Changelog at top documents). Five escalations to Sky in §7.3. Not blocked.

**Riley (User Researcher).** Shipped 4 composite personas (Maya recruiter / Daniel client / Priya peer / Jordan curious) + 3 critical journeys Shamus must guarantee end-to-end (Recruiter Scan / Considered Read / Curious Wander) + cross-cutting friction ranking. Headline finding: ffern aesthetic is **double-edged** — strongly positive for Daniel/Priya, mixed-to-negative for Maya/Jordan unless balanced with proof-above-fold + interactivity. Explicit recommendation to Quinn: "contact-form-only is a P0 risk" — already aligned with Quinn's mailto recommendation. Confidence is medium (no real analytics exist — reasoned hypothesis). Not blocked.

**Rory (DevOps).** Shipped GH-Pages deploy plan modeled on the existing `pacman-code-trainer` success (`skypie99.github.io/pacman-code-trainer/`). Headline finding: deploy is mechanical once Sky completes one-time manual GitHub setup (repo create, Pages source = Actions, optional CNAME). Zero secrets needed — OIDC + built-in `GITHUB_TOKEN`. Wrote shape of `deploy.yml` but **did not author the workflow file** (Constitution Art. 5 — no external side effects, waiting for actual Next.js scaffold to test against). Four escalations to Sky in §7. Not blocked.

**Dana (Backend).** Shipped 3 Zod-shaped schemas (Deliverable, Certificate, Profile) for the file-backed content layer, with strict path conventions, required alt-text rules (enforced by `AltTextSchema` regex), and a **featured-slot integrity rule** that fails the build if multiple deliverables claim `featured: true`. Headline finding: deliberately did NOT touch a database (none exists; Dana's role doesn't touch live DBs anyway); the schema **is** the data integrity contract. Three escalations (MDX-vs-JSON, expiresDate UI, tags vocabulary). Not blocked.

**Shamus (Feature Pusher).** Shipped scaffolding blueprint covering: stack choice (Next.js 15 + Tailwind + Framer Motion v11 + Lucide), init command sequence (for Sky to run NEXT cycle), full annotated file tree, every Dani primitive mapped to a component file + illustrative Tailwind classes, and a Cycles 2–6 build sequence (shell → hero → content layer → certs+about → polish/footer/contact/safety sweep). Headline finding: estimated final bundle <12KB CSS gzip + <30KB JS — well within performance budgets Peter will audit later. Six escalations. Six explicit "what Shamus will NOT do this cycle" items in §9 — clean Constitution rails. Not blocked.

**Alex (Accessibility).** Shipped WCAG 2.2 AA contrast audit (16 pairs, with full math methodology so anyone can reverify). Headline finding: **3 BLOCKERs in Dani's first-draft tokens** — Sage as text was fragile across surfaces; Stone failed 3:1 for interactive borders; Terracotta as link text failed 4.5:1 on every cream surface. All three were resolved within this cycle by a Dani revision pass (see §4 below). Two escalations remain for Sky (Theme B above). Also wrote binding accessibility patterns for sidebar landmark/skip-link, hamburger focus trap, alt-text rules, keyboard rails, motion-reduce coverage. Not blocked at cycle close.

**Will (Tech Writer).** Shipped README.md with repo orientation and recommended reading order, plus LEARNINGS.md skeleton (intentionally near-empty with prompts for what Cycle 2+ should populate). Headline finding: the read-order guidance puts Morgan's briefing first, then PLAN, then personas/features/design — matches Sky's QA report convention. Not blocked.

---

## 4. Cross-role conflicts / handshakes resolved

**Dani ↔ Alex (3 BLOCKERs → resolved within cycle).** This is the headline cross-role outcome of the day.
- BLK-1: Sage `#717267` failed text contrast on Blush/Warm White/Peach Cream (4.46 / 4.26 / 4.14). → Dani replaced `--color-sage` with `#5C5D54` (tuned-darker sage, passes 5.66:1 to 6.33:1) AND kept the original `#717267` as `--color-decorative-sage` for non-text use.
- BLK-2: Stone `#DCDCD6` failed 3:1 as the ghost-button border. → Dani added `--color-border-interactive: #888879` (passes 3.41:1) and reserved the original Stone for purely decorative dividers (WCAG-exempt).
- BLK-3: Terracotta `#B35F32` failed 4.5:1 as inline link text on every cream surface. → Dani applied Alex's Option B: Terracotta stays for graphics (dot, focus ring, CTA bg, large text ≥24px); Umber `#7F4323` (already in palette) becomes `--color-accent-text` for links and the 19px numbered-step numerals.
- Resolution chain: Alex authored ACCESSIBILITY.md → flagged 3 BLOCKERs → Dani revised PROJECT_DESIGN.md (Changelog at top documents) → all 3 blockers cleared at cycle close. Alex's two remaining items (SKY-1 brand split confirm, SKY-2 hairline split confirm) are now Sky-decisions only.

**Quinn ↔ Dana (feature-shape ↔ schema-shape).** Aligned.
- Quinn's F-04 cards expect title/role/tech/year/heroImage → Dana's `Deliverable` type provides exactly those fields with min/max constraints.
- Quinn's F-05 detail page expects summary/gallery/external link → Dana's schema includes gallery (max 8 images, alt required) and a links array with `type` enum and a "only one link of type 'demo' allowed" refinement.
- Quinn's F-02 sidebar Featured slot → Dana's featured-slot integrity rule (exactly one `featured: true` across the corpus, build fails if multiple, sidebar falls back to "Latest Work →" if zero) covers the edge cases Quinn didn't have to spec.

**Quinn ↔ Shamus (F-09 Journal conditional).** Aligned.
- Quinn put Journal at P2 pending Sky decision. Shamus's file tree includes `app/(journal)/` as a **route group** in parentheses (so the URL stays clean) conditional on Sky's go/no-go. Either answer is mechanical — no rework if Sky says no.

**Dani ↔ Shamus (9 design primitives ↔ component tree).** Aligned.
- Every one of Dani's 9 primitives (§3.1 through §3.9) maps to a specific component file in Shamus's `components/` tree, with illustrative Tailwind class composition (SCAFFOLDING_PLAN §5 table). No primitives orphaned; no components without a Dani spec.

**Riley ↔ Quinn (visitor needs ↔ backlog priorities).** Aligned.
- Riley's 3 critical journeys map cleanly to Quinn's P0 features (F-01/F-02/F-03/F-04/F-05 cover Critical Journey 1; F-07/F-08 cover Critical Journey 2; the homepage interactive moment in F-01 covers Critical Journey 3).
- Riley's explicit recommendation against form-only contact matches Quinn's mailto recommendation.

**Riley ↔ Dani (aesthetic tension named).** Surfaced, not yet resolved by design.
- Riley flagged ffern aesthetic as double-edged for Maya/Jordan. Dani's response is the warm-minimalism-PLUS-motion philosophy (§0) — the motion catalog and the bright-cream interpretation directly address Jordan, and the hero mockup's "AI PORTFOLIO — 2026" eyebrow + 16-word headline address Maya's "no proof above the fold" risk. Adequate for Day-0; revisit if real-traffic analytics later show bounce on the hero.

---

## 5. What's queued for next cycle (Cycle 2)

Per Shamus's build order (SCAFFOLDING_PLAN §7), Cycle 2 = **Shell foundation**: sidebar + hamburger + cream canvas + token CSS rendering on every route.

**Tasks for Cycle 2:**
1. Sky runs the §2 init commands in SCAFFOLDING_PLAN (`npx create-next-app@latest .` with the documented flags).
2. Shamus authors `tailwind.config.ts` with Dani's tokens.
3. Shamus authors `app/globals.css` with the verbatim `:root` block from PROJECT_DESIGN §1.1 + reduced-motion media query.
4. Shamus authors `app/layout.tsx`, `components/layout/Sidebar.tsx`, `components/layout/HamburgerNav.tsx`, placeholder `app/page.tsx`.
5. Alex audits hamburger keyboard a11y (focus trap, Escape, skip-link).
6. Gary writes first Playwright test (page loads on cream bg, sidebar visible at 1280px, hamburger opens/closes).
7. Rory authors `.github/workflows/deploy.yml` against the now-real Next.js app.

**DECISIONS FOR SKY that BLOCK Cycle 2 specifically:**
- **A2** (repo name — Rory needs to finalize `basePath`).
- **A3** (stack ratification — Shamus needs Tailwind+Framer Motion confirmed before running scaffold).
- **A5** (`main` lifecycle — Sky needs to merge this cycle's artifacts before Cycle 2 has a baseline).
- **B1, B2, B3** (Dani's revised tokens — already applied, just need Sky's confirm so `tailwind.config.ts` lands the right values first-try).
- **E1** (public repo confirm — Sky needs to create the repo before Cycle 2's first push).
- **E3** (Node 20 confirm — Shamus pins `engines.node` accordingly).

**DECISIONS that can wait past Cycle 2:**
- A4 (MDX vs JSON) — Cycle 4.
- B4 (Brand Extensions) — v2.
- B5 (hamburger touch target) — Cycle 2 implementation detail, Alex/Dani can resolve directly.
- C1 (real deliverables) — Cycle 4.
- C2 (real certificates) — Cycle 5.
- C3 (which featured) — Cycle 4.
- C4 (expiresDate UI) — Cycle 5.
- C5 (tags vocabulary) — Cycle 4.
- D1 (contact form) — Cycle 6.
- D2 (Journal) — Cycle 7+.
- D3 (social profiles) — Cycle 6.
- E2 (custom domain) — any cycle after launch.

---

## 6. How to review

```bash
cd ~/Portfolio

# See the cycle branch state
git status                               # all artifacts are untracked — Sky's first commit captures them
git branch                               # only cycle/auto-2026-05-23 exists; main is unborn

# (Once Sky commits) inspect the cycle log
git log --oneline cycle/auto-2026-05-23
```

**Recommended reading order** (lifted from Will's README.md):

1. `/Users/skypie/Portfolio/qa-reports/cycle-2026-05-23.md` — this briefing. DECISIONS FOR SKY at the top.
2. `/Users/skypie/Portfolio/PLAN.md` — Morgan's cycle plan.
3. `/Users/skypie/Portfolio/docs/PERSONAS.md` — who we're building for.
4. `/Users/skypie/Portfolio/docs/FEATURES.md` — what we're building (DECISIONS FOR SKY block at the top of that doc too).
5. `/Users/skypie/Portfolio/docs/PROJECT_DESIGN.md` + `/Users/skypie/Portfolio/designs/home-hero-mockup.md` — how it looks (note the Changelog at the top of PROJECT_DESIGN documenting Alex's WCAG fixes).
6. `/Users/skypie/Portfolio/docs/ACCESSIBILITY.md` — the WCAG floor and the 3-blocker resolution narrative.
7. `/Users/skypie/Portfolio/docs/DATA_SHAPE.md` + `/Users/skypie/Portfolio/docs/SCAFFOLDING_PLAN.md` + `/Users/skypie/Portfolio/docs/DEPLOY_PLAN.md` — how it gets built and shipped.

**To bless this cycle** (Sky's actions only — per Constitution Art. 1):

```bash
cd ~/Portfolio
git add -A
git commit -m "Day-0 kickoff: planning artifacts (Quinn, Dani, Riley, Rory, Dana, Shamus, Alex, Will, Morgan)"
git branch main cycle/auto-2026-05-23     # creates main pointing at the cycle's tip
# OR — if Sky wants main and cycle separate then merge later:
git branch main                           # creates empty main from current HEAD
# Then merge cycle/auto-2026-05-23 into main when ready
```

The `cycle/auto-2026-05-23` branch can stay as the historical record of this planning cycle. Cycle 2's work will live on `cycle/auto-<next-date>` and merge into `main` again when Sky blesses it.

---

## 7. FAIL_FAST / BLOCKER record

- **3 BLOCKERs raised, 3 resolved within cycle.** Alex's BLK-1, BLK-2, BLK-3 all flagged in ACCESSIBILITY.md §1.3 → Dani applied fixes verbatim per Alex's recommendations → PROJECT_DESIGN Changelog documents the resolution → contrast pairs that were failing now pass with comfortable headroom (e.g., Sage replacement went from 4.46:1 fail to 6.09:1 pass on Blush; Terracotta-as-link replaced with Umber went from 4.33:1 fail to 7.30:1 pass on Cream).
- **No blocker stands at cycle close.** Alex's two remaining items (SKY-1, SKY-2) are confirmation requests for Sky, not engineering blockers.
- **No FAIL_FAST events.** Every role respected Constitution v1.3 rails: no code shipped, no `main` commits, no external sends, no secrets handled, no live surfaces touched.
- **One soft conflict to surface (not a blocker):** Dana recommends MDX for deliverables; Shamus recommends JSON-first defer-MDX. Both are defensible. Decision A4 above. Resolvable by Cycle 4.

---

*Morgan, 2026-05-23 — orchestrator-mode cycle briefing. Authored under Constitution v1.3 Art. 9.4 (no external sends inside orchestrator runs). Briefing lives at `/Users/skypie/Portfolio/qa-reports/cycle-2026-05-23.md` and only there.*
