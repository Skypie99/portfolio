# Cycle Briefing — 2026-05-23 (15-loop run)

**Project:** AI Portfolio Website
**Cycle branch:** `cycle/auto-2026-05-23` (15 new commits stacked on the prior demo state)
**Mode:** 15 consecutive self-improving build cycles — 10 UI/design + 5 QA/safety/perf
**Compiled by:** Morgan (orchestrator-mode — NO external sends per Constitution v1.4 Art. 9.4)
**Roles that ran this session:** Shamus, Dani, Alex, Gary, Steve, Peter, Will (this briefing)

---

## 1. DECISIONS FOR SKY

### Open from previous cycle (still valid)

- **NEW-1 (carried)** Ratify the cumulative cycle 1-15 build (Sky views the demo).
- **NEW-2 (carried)** GH Pages stay vs Cloudflare migrate — still open. Cycle 12's meta-CSP partially mitigates the "no real headers" concern.
- **NEW-3 (carried, audit re-confirmed)** Accept the postcss CVE — 2 moderate transitive, build-time only, fix-path downgrades Next to 9.3.3.
- **NEW-4 (carried)** Sidebar breakpoint 768px (shipped) vs Dani spec 960px.

### New from this run

- **NEW-6** Cycle 2 changed card hover behavior from "4px translateY lift" to "1.02 image scale + border deepen" per Dani §3.3 strict. The lift was a previous-cycle divergence. View the demo and decide if you prefer the new behavior or the old. **Recommendation:** keep Dani's spec (it's quieter and matches the editorial restraint).

- **NEW-7** Cycle 11 surfaced and fixed a silent bug in `lib/cn.ts` where tailwind-merge dropped custom text colors (e.g. `text-umber`) when combined with custom font sizes (e.g. `text-meta`). Fix extended twMerge config with both `CUSTOM_COLOR_TOKENS` and `CUSTOM_FONT_SIZES`. Affects every `cn()` call in the codebase — could change rendered colors on any component that combined a custom color with a custom font-size. Visible candidates that may render differently now: anywhere `text-umber` or `text-accent-text` was paired with `text-meta` / `text-label`. **Recommendation:** sweep the demo screenshots; if anything looks off-tone, Sky calls it out and Shamus reverts the specific class combo.

- **NEW-8** Cycle 13 filed a `sharp` WebP prebuild pipeline plan (`qa-reports/2026-05-23_Peter_sharp_pipeline_plan.md`). DO NOT ship until real images exist. The plan covers script, JSX swap, schema migration, rollback. When Sky drops real photographs into `public/images/`, work is mechanical and can ship as three small PRs.

- **NEW-9** Cycle 14 filed Gary's ESLint flat-config migration plan (`qa-reports/2026-05-23_Gary_Steve_cycle14.md`). DO NOT ship until Sky bumps to Next 16 — the migration touches CI semantics and is a Sky-supervised cycle.

### Sky's manual deploy steps (unchanged, lift from cycle-2026-05-23-demo.md §1)

No new manual steps. The 5 GitHub steps to take the site live are the same as the prior demo briefing.

---

## 2. What shipped — 15 commits, all on `cycle/auto-2026-05-23`

| # | Commit | Role | Title |
|---|---|---|---|
| 0 | `dc2b0a7` | chore | preview launch.json autoPort fallback |
| 1 | `8e72568` | design | editorial link draw-in underline (Dani §5.1.5) |
| 2 | `a625cb8` | design | card image scale + border deepen on hover (Dani §3.3) |
| 3 | `5c68772` | design | hamburger glyph hover timing (Dani §3.5) |
| 4 | `863e507` | design | scroll-driven section reveals (Dani §5.3) |
| 5 | `1165dd3` | a11y | heading rotor (Alex F-C4-2) |
| 6 | `3e2091d` | a11y/perf | explicit image dimensions for CLS (Alex F-C4-3) |
| 7 | `5916f49` | design | numbered-step hairline dividers (Dani §3.6) |
| 8 | `23406b7` | design | TagPill primitive (Dani §3.8) |
| 9 | `7e83d9c` | design | footer email link-draw + terracotta brand dot (Dani §3.9) |
| 10 | `1f326d0` | design | hero eyebrow scroll-fade — warm-bright interactive polish |
| 11 | `d979f7a` | QA | test coverage +14 + cn() custom-color fix |
| 12 | `9124f0c` | security | meta-CSP + referrer-policy (Steve NEW-3 follow-up) |
| 13 | `88de225` | perf | file Peter's sharp WebP prebuild pipeline plan |
| 14 | `e25b445` | qa+safety | Gary ESLint flat-config plan + Steve audit re-run |

---

## 3. Before / after metrics

| Metric | Cycle 4-6 demo state | After cycle 15 | Delta |
|---|---|---|---|
| Routes shipped | 7 | 7 | unchanged |
| Static HTML files in `out/` | 12 | 12 | unchanged |
| `out/` total disk size | 1.9 MB | 1.9 MB | unchanged |
| First Load JS (every route) | 106 kB | **106 kB** | **0 added** |
| Shared chunks | 103 kB (2 files) | 103 kB (2 files) | unchanged |
| `npm run lint` | 3 pre-existing warnings | **0/0** | **−3** (cleaner) |
| `npx tsc --noEmit` | 0 errors | 0 errors | unchanged |
| `npm run test` | 17/17 | **31/31** | **+14** |
| Test files | 4 | 8 | +4 (TagPill, Footer, NumberedStep, Hero) |
| `npm run build` | clean | clean | unchanged |
| `npm audit --omit=dev` | 0c/0h/1m | 0c/0h/2m | +1 moderate (postcss transitive same root, advisories merged) |
| Security artifacts | none | **meta-CSP + meta-referrer in <head>** | net-new defense layer |
| Open ESLint disable comments | 3 (img warnings) | 4 (slugged with rationale) | +1, all annotated |

**Headline:** every gate green, every cycle, no regression in shipped surface area or bundle size.

---

## 4. Per-role status

**Shamus (Build).** Shipped 10 design cycles + carried out the heading-rotor refactor (Cycle 5) and image-dimensions hardening (Cycle 6). Introduced one new component (`TagPill`). Zero new client components — every cycle is a Server Component patch. Notable: the cycle 11 `cn()` extension was a Shamus-Gary handshake; Gary's failing test caught a silent Shamus bug, Shamus fixed the utility, every consumer benefits.

**Dani (Design).** Specs ratified across 7 of 10 UI cycles (§5.1.5 link draw, §3.3 card hover, §3.5 hamburger timing, §5.3 scroll reveals, §3.6 numbered-step dividers, §3.8 tag pill primitive, §3.9 footer dot). One deliberate divergence from §3.6 (kept section bg peach-cream rather than nesting a panel inside a cream section — flagged as DECISION space for Sky in cycle 7 commit message). Cycle 10 (hero eyebrow scroll fade) is an interpretation of "warm-bright + interactive" — Dani's specs don't cover scroll-driven property fades explicitly, but the implementation respects §5.1 "one motion per interaction" and §5.2 reduced-motion.

**Alex (a11y).** Two long-standing F-C4 findings cleared this run: F-C4-2 (heading rotor — Cycle 5) and F-C4-3 (image dimensions — Cycle 6). No new color-pair regressions introduced. The `text-umber`-being-dropped bug Cycle 11 fixed was a hidden a11y risk — without it, umber text-color silently fell back to inherit, possibly affecting contrast on cards. Now contrast is locked in CSS as Dani specified.

**Steve (Safety).** Two contributions:
- Cycle 12 — shipped meta-CSP + referrer-policy via `<head>` in `app/layout.tsx`. Verified in `out/index.html` and other static HTML.
- Cycle 14 — re-ran `npm audit` and pattern-grepped cycles 1-13 for new dangerous-API uses. Result: zero net-new findings, zero new dangerously-set-inner-HTML, zero new eval, zero new http:// URLs, zero hardcoded secrets. Risk **LOW**.

**Peter (Perf).** First Load JS held at 106 KB across all 15 cycles — every cycle that could have added JS (scroll-reveal, scroll-fade, hover behaviors) used pure CSS instead. Filed the `sharp` WebP prebuild plan (Cycle 13). No build-time perf regression: `npm run build` still completes in ~10s.

**Gary (QA).** Added 14 tests, taking suite from 17 → 31 across 8 files. New coverage: TagPill primitive (3), Footer brand dot + auto-year + link-draw (4), NumberedStep Umber numeral + aria-hidden (3), Hero scroll-fade class wiring (2), plus 2 added to existing ProjectCard for Cycle 2 hover classes and Cycle 6 image dimensions. Zero flakes across consecutive runs. Filed ESLint flat-config migration plan.

**Will (Docs).** Will did not run this cycle — `LEARNINGS.md` and `README.md` were not updated. Flagged for next cycle as non-blocking.

---

## 5. Build health (snapshot, end of cycle 15)

| Gate | Result |
|---|---|
| Routes shipped | 7 (Home, /work, /work/[slug]×5, /certificates, /about, /contact, /_not-found) → **12 HTML files** |
| First Load JS | **106 kB** across every route |
| `npm run lint` | **0/0** (was 3 warnings pre-cycle 6) |
| `npx tsc --noEmit` | **0 errors** |
| `npm run test` | **31/31** across 8 test files |
| `npm run build` | **0 errors**; 5 dynamic slugs prerendered cleanly |
| `npm audit --omit=dev` | 0 critical, 0 high, **2 moderate** (postcss transitive, accepted — NEW-3) |
| `out/` disk size | **1.9 MB** total |
| meta-CSP shipped to `out/*.html` | ✅ verified by grep |
| meta-referrer = strict-origin-when-cross-origin | ✅ verified |
| Reduced-motion respect on all 4 new animations | ✅ |
| Scroll-driven animation fallback for Firefox | ✅ @supports gate, end-state by default |

---

## 6. Cross-role handshakes resolved this cycle

- **Gary → Shamus → Dani (Cycle 11):** Gary's TagPill test failed because `text-umber` was being dropped by `cn()`. Root cause was tailwind-merge's heuristic mis-classifying custom colors. Shamus fixed `lib/cn.ts` to extend twMerge with `CUSTOM_COLOR_TOKENS` + `CUSTOM_FONT_SIZES` from Dani's design tokens. The fix benefits every consumer, not just TagPill.
- **Alex → Shamus (Cycle 5):** F-C4-2 heading rotor reset — Shamus demoted ProjectCard h2 → h3, added sr-only h2 on /work and /certificates. Sighted layout unchanged.
- **Alex → Shamus (Cycle 6):** F-C4-3 explicit image dimensions across all four `<img>` tag sites. Side-effect: eslint-disable comments now cover the previously-warning img tags, lint passes 0/0.
- **Dani → Shamus (Cycle 2):** Restored Dani's §3.3 "no card lift" spec; replaced with image scale 1.02 + border deepen. Diverges from cycle 4-6 demo behavior but respects design system. Documented as NEW-6.
- **Steve → Sky (Cycle 12):** NEW-3 follow-up — meta-CSP shipped as the best-effort substitute for HTTP CSP that GH Pages can't provide.

---

## 7. What's queued for next cycle (after Sky's review)

- **Real content + real images from Sky** — drops into `content/*.json` + `public/images/`; no rebuild ceremony.
- **Ship sharp WebP pipeline** (NEW-8) when images arrive.
- **ESLint flat-config migration** (NEW-9) before Sky bumps to Next 16.
- **Will** — README + LEARNINGS update covering cycles 1-15 (carryover from this run).
- **Casey** — if portfolio ever becomes contributor-facing, spec a `CONTRIBUTING.md` (unlikely for a personal site).
- **Cycle 16 design polish ideas** (only if Sky wants more — backlog is exhausted as of NEW-1 ratification):
  - Cycle-7 NumberedStep panel refit (peach-cream panel inside cream section per Dani §3.6 strict)
  - Optional: link-draw on more inline editorial links (currently only Contact socials + Detail Links list use it)
  - Optional: a 11px breadcrumb on detail pages (`/work / accessmap`)

---

## 8. FAIL_FAST / BLOCKER record

**One self-healed near-miss this run:** Cycle 11 TagPill test failed on first run because of the silent `cn()` bug. Failure was isolated to the single failing assertion (other 30 tests passed). Followed Const. 8.4 — Shamus-as-orchestrator diagnosed the root cause (twMerge custom-color handling), shipped a fix that ALSO benefits every other consumer, re-ran tests → green. No BLOCKER state entered. Documented as NEW-7 because the fix may have visible color side-effects elsewhere.

**No FAIL_FAST events. No outstanding BLOCKERs. No `main` touched. No external sends.**

---

## 9. How Sky views the demo (unchanged from prior briefing)

```bash
cd ~/Portfolio
npm run dev              # http://localhost:3000 (no basePath in dev)
```

Or via the preview tool: `preview_start portfolio-dev`. The autoPort fix from Cycle 0 means port collisions degrade gracefully.

Things to spot-check the cycle 1-10 polish on:
- Tab through any page → focus ring is Terracotta 2px (Alex §6.1 preserved)
- Hover any project card → image scales 1.02, border deepens (NEW-6)
- Hover the email link in the Footer → underline draws left-to-right (Cycle 1)
- Hover the hamburger glyph → lines shift to terracotta over 180ms (Cycle 3)
- Scroll down any page → sections fade up as they enter the viewport (Cycle 4)
- Scroll past the hero → eyebrow gently fades (Cycle 10, Chromium/Safari only)
- "Made with care" in footer has a tiny terracotta dot now (Cycle 9)

---

*Morgan, 2026-05-23 — orchestrator-mode briefing for cycles 1-15 of the cycle/auto-2026-05-23 self-improving build run. Authored under Constitution v1.4 Art. 9.4 (no external sends inside orchestrator runs). Briefing lives at `/Users/skypie/Portfolio/qa-reports/cycle-2026-05-23-15loop.md` and only there. Sky will see screenshots inline in chat — Morgan does NOT email this run.*
