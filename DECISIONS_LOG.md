# DECISIONS_LOG — portfolio

| Decision | Date |
|---|---|
| `[portfolio-merge-approved]` User approved merge to main despite Constitution Art. 1 hard rule — Sky's intent overrides protocol | 2026-05-25 |
| `[all-links-current]` Portfolio links are production-ready; iMessage confirms trio of live sites | 2026-05-25 |
| `[five-live-six-shown]` Copy says SIX projects built/shown, FIVE live; Mutual Mesh built but NOT live. Hero "Six projects built, five live"; showcase eyebrow "Shipped" + "Built, shipped, and open."; footer "five of six live"; Mesh demo link reads "Demo" (card + detail + a11y label). Sky-approved. | 2026-06-10 |
| `[card-reorder]` Work list reordered — AccessMap featured #1, Claude Corp #2, Dashboard #3, rest unchanged. One `deliverables.json` source → homepage grid + `/work` agree; Mutual Mesh stays the full-width bookend. Sky-approved. | 2026-06-10 |
| `[final-polish-merged]` `polish/portfolio-final-2026-06-10` merged to main `--no-ff` → `80e512f`. Rollback `git revert -m 1 80e512f`. Intro byte-identical; lint/typecheck/184 tests/build/static all green on branch and main. Sky authorized merge (intent overrides Const. Art. 1). | 2026-06-10 |
| `[register-fork-r3pr-no-fire]` Conditional `03_R3-PR_register-mockups.md` fired → entry gate held (R3 DECISIONS #1 = OPEN, no (b)-pick / (a2) ask on disk) → NO-FIRE, zero edits, window closed. Sky confirmed the close in-window; fork stays OPEN default (a)-INFUSE at her leisure. Halt record: `~/.claude/plans/r3-pr-register-fork-radiant-ullman.md`. | 2026-07-16 |
| `[r3-deploy-executed]` R3 arrival train SHIPPED: Sky authorized in-chat ("You run them, Claude" — intent overrides Const. Art. 1, same precedent as 06-10); local ff to `544dc1b` was already done by Sky's hand; Claude pushed `22d98f8..544dc1b`, deploy run `29548542616` SUCCESS, handshake 5/5 PASS. **Sky also explicitly SKIPPED the optional A0/R3-D2 pre-fix leg → A-03 permanently rests MEDIUM-on-outcome** (pre-fix build gone from production; R3 DECISIONS standing rule #7). Remaining: the ONE device session (R3-D5 headline) + T7/T9/T18 fills + C-44. | 2026-07-16 |
| `[room-shipped]` THE ROOM merged + pushed live (`a58ecf1`). All phases but **F (art wing, HELD)**. Sky authorised merge+push in-chat under the 2026-08-25→08-27 sprint merge exception; what would go live was named first and the deploy verified after (CI success → Deploy success, run `33011970899`; 25 URLs 200, 0 console errors, one `<h1>` on 16/17). | 2026-08-26 |
| `[round-iv-closed-v-open]` `content/rounds.json`: Round IV "Weight" closed `2026-08-26` (`15 proposed · 9 chosen` kept verbatim, `9 shipped · 9 accounted` appended — append-only). Round **V "The Room"** opened: `11 phases · 10 shipped · 1 held` + `axe 0 violations · 34 scans`. It is genuinely open because Phase F is held; it closes when F fires. | 2026-08-26 |
| `[global-error-copy-ratified]` `app/global-error.tsx`'s eyebrow/heading/body adopted AS WRITTEN. They shipped 2026-07-04 marked "replace before merge", merged with the TODOs in, and sat live on every route's crash path for seven weeks in no ledger. Sky read them and ratified; not one visible character changed. | 2026-08-26 |
| `[humans-txt-stripped]` `public/humans.txt`'s "NEEDS-SKY COPY (placeholder — not final)" header and its empty `/* NOTES */` section removed. It had been serving publicly, linked from `<link rel="author">`. Everything remaining was already true and already Sky's. The optional NOTES line stays hers to add or leave absent. | 2026-08-26 |
| `[bp4-lamp-as-built]` The dark-theme exhibit lamp stays **CONFINED** to the media well. Both variants were built, captured at 1440/375 in both themes, and the experiment fully reverted. The spill version's gain is marginal; `overflow-visible` on that well is shared, load-bearing layout across all six case studies (the settle and the sticky both depend on it). | 2026-08-26 |
| `[intro-cue-rise-unchanged]` `intro-cue-rise` stays at 600ms + its 700ms delay. PROTECT-66 sanctions the cue's EXIT duration only; its entrance is protected, and the guard bank records two refinements of exactly this kind being reverted as governance violations. The ban list outranks the work list. Both values are enumerated survivors in the duration guard with the reason attached, so they are documented rather than drifting. | 2026-08-26 |
| `[browserslist-unchanged]` `browserslist` stays `defaults, not dead, not op_mini all`. The motivating premise was measured and FALSE: Next's `polyfills.js` is a fixed framework bundle and came back **byte-identical** (112,594 B, same hash) under a tighter modern target, as did every route's First Load JS. The only real effect was −2,899 B of CSS. Not worth narrowing who can load the site for ~3KB. | 2026-08-26 |
| `[tree-budget-scope]` The ≤8MB tree budget **excludes noindexed, unlinked pages**. `public/` is 25.65MB raw but **8.42MB** without `public/videos/amazon-night-flight.mp4` (17.23MB, 67% of the tree), which belongs to `/runway`: `robots: noindex`, in no nav/footer/sitemap, `preload="metadata"`. The budget protects the crawlable visitor experience and that asset structurally cannot affect it. | 2026-08-26 |
| `[fable-s16-superseded]` Fable S16's "carry-away artifact" fork (résumé / LinkedIn / status quo) is **superseded** by the Record band + the hero receipts: the artifact is published on the page rather than downloaded. Sky ratified the supersede — it had been carried as absorbed since `11_ROADMAP.md` without the ratification ever being recorded. | 2026-08-26 |
| `[archive-h1-unlocked]` One element of the PROTECT-read-only `/archive` island unlocked: `AuthGate`'s `sa-card-kicker` `<div>` → `<h1>`. Live axe found the real sign-in card shipping zero headings; no local scan could see it, because without a `.env.local` the route falls to `global-error.tsx`, which HAS an h1. Verified pixel-identical by computed-style diff against live. New guard `components/archive/__tests__/AuthGate.test.tsx` (3 tests, mutation-tested). The island stays read-only otherwise. | 2026-08-26 |
| `[about-method-draft-a]` `/about#method` adopts **Draft A** — the band defers to the front page's canonical `#how-i-work` account instead of paraphrasing it in a third shortened form. Eyebrow "Method" and the section id kept (the rail's byte-exact label, guarded by section-nav-anchors T2). Phase C drafted it and deliberately left it unapplied; Sky picked A. | 2026-08-26 |
| `[trim-hero-weight-unlocked]` `components/cinematic/StaticDesertFrame.tsx` unlocked for `perf/trim-hero-weight` only — the mobile-tier `srcset` so phones stop downloading desktop-magnification cinematic plates. Claude resolves the conflict and prepares the branch; Sky merges. `components/cinematic/**` stays project law otherwise. | 2026-08-26 |
