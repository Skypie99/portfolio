# THE ELEVATION LEDGER — luxe-per-effort, build-card ready (2026-08-07 · [Fable 5])

**Scope law honored:** Sky's range was 10–20; the honest count is **10** — every item below justifies itself alone; nothing is padding. All items are NEW work beyond the unmerged polish stack, the R2–R4 slates, the truth queue, and every open Sky pick (dedup register: `00_dedup-register.md`; collisions cited per item). All items are INFUSE-register (R3 fork #1 default respected), branch-stopped, Sky-merged.

**Standing floors for every item (never break):** axe strict 0 · CLS ≤ 0.004 · RM 6-layer contract (reduced motion = designed peer) · intro-lock untouched · C3 byte-lock · T7/T12/T18 placeholders untouched · no copy/voice authored by agents · Sky-only merge/push · the PROTECT list (r2-slate §Stage 2) + this audit's PROTECT adds (sepia shadows · one-width borders · six-radius roles · duration/easing scale · stagger ladder · hung eyebrow bullets · optical ±15px pulls · focus-ring tokens · gold selection).

**Recommended waves:** Wave 1 = items 1–5 (one sitting, all XS/S). Wave 2 = 6–8. Wave 3 = 9–10.

---

## 1. `curly-the-estate` — smart punctuation at render
- **Lens:** 2 (typography) · **Locations:** everywhere content strings render (about intro, footer "Let's talk" + tagline, contact eyebrow, flagship prose, home; the 404/blog-h2 already use ’)
- **CHEAP TELL (measured):** typewriter `'` in luxury prose — 5 sightings on home, 4 on /about/ ("weren't", "no one's left out"…), the LET'S TALK eyebrow itself — while other rooms use proper `’`. Mixed = unnoticed, the worst read.
- **EXPENSIVE MOVE:** one render-time smart-punctuation transform (apostrophes; quotes/ellipses covered for the future) where JSON strings meet JSX — zero content bytes changed, zero voice touched; add a vitest guard asserting no U+0027 renders in prose.
- **Effort:** XS–S · **Deps/PROTECT:** no `content/*.json` edits (the no-copy convention stands); UP-44/truth wording untouched (this changes glyphs, not words) · **MOCKUP-GATE:** no
- **Never break:** the byte-locked C3 string renders through the same path — transform must be glyph-only; wrap tests (UP-12/14) stay green.

## 2. `finish-the-glass` — the light glass joins the warm palette
- **Lens:** 4 (material) · **Locations:** `.glass-card` light theme — the identity chip (the first pixel of every arrival) + 6 work cards + glass pills
- **CHEAP TELL (measured):** pane tint `rgba(252,251,255,.42)` (#FCFBFF, blue-white) + corner glow `rgba(150,188,214,.26)` — a cool material floating on the warm canvas (#FAF8F1), while the estate's own ruling (UP-19, unmerged `8645610`) already warms the *rim*. Post-merge the material splits: warm rim on cool pane.
- **EXPENSIVE MOVE:** pane tint + corner-light stop move to the warm family (one token + one gradient stop), completing UP-19's thesis — candle-warm glass estate-wide in light.
- **Effort:** S · **Deps/PROTECT:** ships AFTER the polish merge (stacks on UP-19); the derived ink-contrast guard (`e85a93f`) must re-run — pane alpha feeds composited backdrops; axe 0 floor · **MOCKUP-GATE: YES** (cool-cast glass is defensible material realism — render warm vs current for Sky's eye)
- **Never break:** glass legibility ratios; dark glass untouched (already warm).

## 3. `the-hand-answers` — the two dead sidebar links learn the house grammar
- **Lens:** 7 (micro-states) · **Locations:** sidebar FEATURED project block + "Read the notes →", every subpage
- **CHEAP TELL (measured):** 41/44 interactive elements respond to hover; these two (plus the intentionally focus-only skip link) give the hand nothing — on the estate whose grammar is "everything quiet answers when touched" (18/18 content links draw, scrollbar thumb has a hover).
- **EXPENSIVE MOVE:** the inner label takes the link-draw underline + the → glyph's existing nudge grammar on block hover/focus-visible; nothing louder.
- **Effort:** XS · **Deps/PROTECT:** UP-36 (Notes link *color family*) is Sky's open pick — this item changes response, not color; build card must flag the adjacency so the pick isn't preempted · **MOCKUP-GATE:** no
- **Never break:** 44px targets; focus-visible parity with hover.

## 4. `a-drawn-hamburger` — the last default glyph joins the drawn family
- **Lens:** 7/9 · **Locations:** mobile chrome, above the fold on every first load
- **CHEAP TELL:** three straight default lines sharing the frame with a drawn sun (favicon/chip) and a ratified drawn chevron (UP-39) — the one glyph no hand drew, at the most-seen altitude.
- **EXPENSIVE MOVE:** a hand-weighted mark in the sun/chevron stroke family (subtle taper/optical weighting; same 44px disc, same aria, same dialog).
- **Effort:** XS–S · **Deps/PROTECT:** D3 (dialog device row) behavior untouched · **MOCKUP-GATE: YES** (2–3 candidates rendered for Sky)
- **Never break:** tap target, contrast on film frames (the disc scrim stays).

## 5. `rag-finish` — the reading rooms take their last polish
- **Lens:** 2 · **Locations:** prose paragraphs estate-wide; the two 76–79cpl runs (process steps, one /about/ block)
- **CHEAP TELL (measured):** `text-wrap: wrap` on prose while heads already run `balance`; two reading runs sit just over the 66–75cpl luxury band (76–79cpl @16–17px).
- **EXPENSIVE MOVE:** `text-wrap: pretty` on prose (progressive, zero-risk) + max-width nudge on exactly those two runs (~640→612px).
- **Effort:** XS · **Deps/PROTECT:** UP-32 one-eyebrow-line rule + wrap guards stay green; 0-CLS discipline (measure the nudge) · **MOCKUP-GATE:** no
- **Never break:** existing balanced heads; overflow census 0.

## 6. `every-room-its-postcard` — the unfurl layer becomes uniformly art
- **Lens:** 8 (seams) · **Locations:** /certificates/, /contact/, /about/, /colophon/, /blog/ OG images (today they share the homepage postcard; the 6 work rooms already have their own)
- **CHEAP TELL (measured):** og:url/titles are per-room on live (TA-10 substantially healed), but five rooms still unfurl the homepage's card — a shared business card where the work rooms hand out engraved ones.
- **EXPENSIVE MOVE:** per-room postcards from the *existing* og-card factory (`og-cards.mjs`, showcase train) — same paper/rule/sun grammar, room-specific line (existing title strings only, no new copy).
- **Effort:** S · **Deps/PROTECT:** stacks after `e12107e` (carried TA-10 commit) — respect its precedence chain; existing strings only (no wording) · **MOCKUP-GATE:** no (grammar exists; factory is deterministic)
- **Never break:** 1200×630, `image/*` content-types, unfurl verification walk (W0).

## 7. `beyond-the-floor` — contrast preferences as designed experiences
- **Lens:** 5/7 · **Locations:** estate-wide token layer
- **CHEAP TELL (measured):** `prefers-contrast: more` rules: **0**; `forced-colors` rules: **0** — on an estate whose thesis is accessibility-as-craft and whose register speaks to RM readers by name. The floors are met (AA everywhere, 5.9–14.7:1); the *preferences* go unanswered.
- **EXPENSIVE MOVE:** a `prefers-contrast: more` token override (ink-boost: muted/meta step toward ink; hairlines toward line-strong) + a forced-colors sanity pass (system-color borders where backgrounds vanish) — the same "designed peer, not absence" philosophy the RM contract already proves.
- **Effort:** S · **Deps/PROTECT:** token-layer only; ink-contrast guard extended to the new layer · **MOCKUP-GATE:** no
- **Never break:** default-theme rendering byte-identical when no preference is set.

## 8. `the-dusk-turn` — the theme flip becomes a dissolve
- **Lens:** 6 (motion) · **Locations:** the theme toggle, every room
- **CHEAP TELL (observed):** the toggle hard-cuts between fully-composed rooms — the one transition on the estate with no choreography, on a site that owns a day/night sky token system and root view-transitions already.
- **EXPENSIVE MOVE:** wrap the flip in the existing View Transition machinery — a ~400ms cross-dissolve on the entrance easing; **reduced motion keeps today's instant cut** (the RM contract IS the fallback).
- **Effort:** S · **Deps/PROTECT:** R4's dark-register cap was train-scoped but its spirit is cited — this adds no new night *material*, only the turn between existing states; theme persistence + no-FOUC inline script untouched · **MOCKUP-GATE: YES** (motion taste — capture both, Sky rules)
- **Never break:** instant RM path; toggle aria/labels; the 180–1200ms duration scale (pick from it, don't invent).

## 9. `gecko-parity-scrollbar` — the jewelry survives Firefox
- **Lens:** 7 · **Locations:** global scrollbar styling
- **CHEAP TELL (measured):** the styled thumb (pill, tokened, hover state) is `::-webkit-` only; Firefox readers get stock UA chrome on an estate whose every other edge is finished.
- **EXPENSIVE MOVE:** two lines — `scrollbar-color: rgb(var(--rgb-line-strong)/.45) transparent; scrollbar-width: thin;` on `:root` (+dark values).
- **Effort:** XS · **Deps/PROTECT:** none · **MOCKUP-GATE:** no
- **Never break:** Chromium styling (webkit rules win there).

## 10. `prints-like-a-book` — the estate on paper
- **Lens:** 8 (seams) · **Locations:** @media print layer; priority order: flagship case study, /blog/ post, /about/, home
- **CHEAP TELL (measured):** print rules: **0**. A recruiter's ⌘P on the flagship prints sidebar chrome, dark backgrounds at the UA's mercy, glass panels as gray soup — the one rendering of the site nobody designed, on the surface most likely to be *filed*.
- **EXPENSIVE MOVE:** a designed print layer in the house grammar — ink-on-paper reset (light tokens forced), chrome hidden, plates/ledgers kept, `@page` margins, external link URLs revealed in mono small caps after labels, the postmark as the printed footer.
- **Effort:** M (per-room care + verification) · **Deps/PROTECT:** print forces light tokens (no dark ink-on-dark); no content reflow that breaks the reading order · **MOCKUP-GATE:** no (print-preview captures banked as receipts)
- **Never break:** screen rendering (print layer must be inert on screen); zero new screen CSS specificity.

---

# THE RESTRAINT LIST — elevations considered and REJECTED (knowing when to stop is the aesthetic)

1. **Re-lighting the desktop arrival's fog-first frames** — inside the locked cinematic intro (R3 U1 / intro-lock). Banked as a QUESTION for Sky (CLOSEOUT Q1), never as work.
2. **Filling the sidebar's 500px of sage** — R4 curation already considered and killed sidebar-content concepts (P14 styled-feed, P10 source-colophon); the void is the quiet stance. Cited, closed.
3. **Softening the SCROLL cue's scrim** — the scrim is load-bearing over arbitrary film frames; the cue survived two adjudications (T6 whisper-weight spec, UP-39 ratified). Leave the instrument alone.
4. **Optical ink-edge hanging for display serifs** — no measured misalignment worth a pass at any sampled size; this would be luxury-cosplay, not craft.
5. **Killing or re-timing the stat count-up** — R3 decision #2 ruled OPTION (i), settled-number rest state. Decided; the walk respects it.
6. **`:visited` styling** — the portfolio convention omits it deliberately; adding it buys nothing and risks the ink system.
7. **A strict modular type ratio** — the quasi-1.5 display ladder + named utilities already read intentional; forcing a mathematical ratio would churn 30 files for a difference no eye can name.
8. **Clamping every prose run to ≤75cpl estate-wide** — fights composed layouts for a marginal gain; only the two measured outliers earn the nudge (item 5).
9. **Replacing the AI-made film plates with photography** — the plates ARE the thesis, art-directed and graded; swapping them concedes the skeptic's cheapest jab and betrays the register.
10. **A light-theme lamp** — the lamp's intensity/placement is Sky's open dial (BP4 fork); extending it to light is her call, not an audit's.
11. **New night-register moves beyond the dusk-turn** — R4's cap (night set = P01+P06+P08) stands in spirit; item 8 transitions between existing states and goes to her eye gated; nothing else at night.
12. **Animating the favicon / dark-variant favicon** — the warm mark holds on dark tabs at 16px (observed); a variant is complexity without a visible win.
