# LENS 1 — THE THREE-SECOND SQUINT (banked 2026-08-07)

**Method:** live skypistudio.com, fresh loads, first-paint + settled-arrival captures. Phone 375×812 then desktop 1280×800, dark then light (OS scheme emulation). Chromium only.
**Pane caveat (applies to the whole run):** the Browser pane ran hidden overnight; scroll-driven states below the film do not composite in captures (stale-layer artifact — same class the estate documented at UP-16). Arrival/first-load captures ARE reliable (verified against layout truth). Below-film home visuals in later lenses are measured via computed style + rects, cross-checked on subpages that paint fully; anything pixel-judged from measurement alone is tagged [MEASURED-ONLY].

## The verdict before any word is read

**Expensive. The arrival is the site's strongest three seconds and its riskiest.** All four configurations open on a full-bleed graded desert vista — no headline, no pitch, no nav bar, one glass identity chip (top-left), one SCROLL cue (bottom-center), hamburger (mobile only). The composition is an art-book cover, not a template hero. Density: near-zero — exactly the register's weight-and-wonder. Nothing above the fold asks for anything.

## What the three seconds actually contain (measured)

- The 4 film plates (mid-sky/mid-mid/arrival-cliff/mid-fg AVIFs) are painted within ~160ms of load — the fog-first frame is NOT network lag; it is the film's own designed mist-lift (`[VERIFIED: resource timings]`). The vista fully resolves by ~2.5–3s (frame-pump observation).
- **Phone first frame (both themes): structured** — clouds, monolith tips, (light) the warm desert floor. The squint reads "place, light, intent" from frame one.
- **Desktop first frame (both themes): near-structureless** — a brown/mauve haze gradient for up to ~2s before the vista resolves. In a hostile 3-second squint, desktop-first-frame is "beige blur," and the two designed objects carrying the whole first impression are the identity chip and the SCROLL pill.
- **Judgment (VIBES, tagged):** the mist-lift is a curtain-rise and it lands as luxury *if the viewer gives it 3 full seconds*. The risk is confined to desktop's first ~2s. **This is inside the locked cinematic intro (arrival choreography = R3 U1 / intro-lock, PROTECT).** → NOT a ledger item; banked as a question for Sky (see CLOSEOUT Q-bank: "does the desktop arrival want one visible subject from frame 1 — e.g. the cliff silhouette held through the mist — or is the pure fog the point?"). No third-party fix is proposable under the lock.

## Where the eye snags

1. **Dark/phone:** the SCROLL cue's scrim pill is the visually heaviest object on the canvas — a near-black blob on pale mist. It must survive every film frame (its scrim is load-bearing over arbitrary imagery), and the chevron/cue was adjudicated (UP-39 ratified, T6 whisper-weight spec, D22 device row). → KNOWN-DECIDED; logged for the Restraint List, not the ledger.
2. **Light/phone+desktop:** the identity chip's glass reads faintly COOL (pale lavender-pink) floating on a hot desert scene — a temperature seam at the single most-looked-at pixel cluster. Dark's smoke-glass chip is seamless. → carried to Lens 4/5 for measurement (chip glass tint vs the warm palette; adjacency: UP-07 ratified the sun-glyph hex, UP-38 covers subpage chips — neither adjudicated the *light-theme film-overlay* chip glass).
3. **Mobile both themes:** the hamburger — three default straight lines in a disc — is the only undesigned glyph above the fold, sharing the frame with a drawn chevron and a crafted sun glyph. → carried to Lens 7/9; NEW candidate (no prior item touches the hamburger's glyph; D3 covers its dialog behavior only).

## The single cheapest-reading pixel above the fold

**Desktop:** the first-frame featureless haze (protected, banked as question). **Mobile:** the hamburger glyph (candidate). Everything else above the fold survives the squint clean.

## Subpage arrival spot-check (/work/, light, desktop — paints fully)

Sage sidebar (serif name, mono tagline, theme toggle, FEATURED/NOTES, WRITE TO ME pill) + cream canvas + terracotta serif display "The Work" with hairline left rule + museum-numbered wall below. Composition: gallery placard — expensive, coherent with the film's register. Eye snag: the sidebar's long empty sage run between NOTES and WRITE TO ME at 1280×800 (~500px of void) — silence vs vacancy question → Lens 3 measures it.

## Lens-1 outputs

- Ledger candidates seeded: hamburger glyph (→L7/L9) · chip light-glass temperature (→L4/L5) · sidebar void (→L3).
- Restraint-list seeds: SCROLL-cue scrim weight (load-bearing over film frames) · desktop fog-first arrival (locked; question banked instead).
- ALREADY-FIXED-UNMERGED observed this lens: the `frame-ancestors` console error fires on every route load on live (UP-01 cures; commit `40c1475`).
