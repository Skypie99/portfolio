# LENS 5 — COLOR AS CRAFT (banked 2026-08-07)

**Method:** token vocabulary (243 root / 52 dark overrides), rendered color censuses per theme (html.dark flip), WCAG arithmetic on the shipped pairs. Live `45f6632`.

## Palette restraint (measured)

- Rendered text colors: **11 (light) / 14 (dark)**; backgrounds 19/20 — for an estate with a film, glass, status systems and six project cards, this is *restrained*. Hue families in play: sage-ink (the inks are green-cast — desert sage, not gray: 32,48,44), terracotta accent, teal secondary, gold, rose, foil-lavender, paper/earth. Seven families + true paper. The palette IS the place's palette.
- Dark is not inverted-light: accent lightens and warms to peach (`231,181,147` accent-ink), shadows deepen by token, paper surfaces **deliberately don't flip** (museum plate/badge paper carry light ink in dark — correct material logic; the dark half of this system (UP-17) is AFU).

## Temperature coherence

- **Light: warm canvas + sage inks + terracotta = coherent** — except the cool glass family (pane #FCFBFF + blue corner glow), already promoted to a ledger candidate in Lens 4.
- **Dark: cool-smoke base canvas (21,25,26) with warm alt bands (34,28,22)** — the candlelit alternation. On live, band separation leans on lightness (the UP-20 defect); the warmth separation fix is AFU (`fbd76bf`). Nothing new to propose — the merge is the fix.
- The status green (`77,169,120`) is the one hue that never flips between themes: semantic constancy for "live" — correct choice, noted not flagged.

## Semantic consistency

Same meaning = same color holds everywhere sampled: terracotta = interactive/CTA/underline; teal = secondary info voice; gold = highlight material (::selection both themes, glow washes); green = liveness. The known open questions in this space are Sky's picks, not drift: UP-36 (Notes link cool vs accent family), UP-11-SET (footer GitHub accent), UP-26 (outro near-black contract).

## Contrast as craft (computed on shipped pairs)

| Voice on canvas | Light | Dark |
|---|---|---|
| Body ink | **13.0** | **14.7** |
| Muted | 7.5 | 9.6 |
| Meta (11px mono) | 5.9 | 7.8 |
| Accent-ink links | 6.7 | 9.6 |
| Cool-deep | 7.6 | 11.0 |

Every reading voice clears AA with luxury margin — the hierarchy is built from *sufficient* contrasts stepped comfortably apart, not from skirting the floor. One arithmetic footnote: mono caps on the accent CTA gradient measure 3.99:1 against the mid stop but ride a ramp whose darker stops carry it, and the strict-axe 0 line stands on live — recorded, not flagged.

## Lens-5 outputs

- No new ledger item beyond Lens 4's glass-warmth completion (which is where light-theme temperature actually leaks).
- AFU observed: UP-20 (dark band warmth) · UP-17 (badge paper dark) · UP-21 (pull-quote ornament dark).
- Known-open cited: UP-36, UP-11-SET, UP-26/27 — all Sky picks; untouched.
